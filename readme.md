# development

```sh
pnpm add  whatsapp-web.js qrcode-terminal puppeteer dotenv @google/generative-ai langchain fs nodemon

pnpm add openai @google/generative-ai puppeteer whatsapp-web.js qrcode-terminal dotenv fs @langchain/textsplitters	

pnpm add langchain

pnpm add @langchain/google-genai @langchain/core @google/generative-ai
```
